from setuptools import find_packages, setup

setup(
    name='RatNet',
    version='0.2',
    description='RatNet',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
